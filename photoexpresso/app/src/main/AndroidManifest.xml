<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.barloufcorporation.photoexpresso">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.MANAGE_DOCUMENTS"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="LoginActivity"
            android:label="Ecran de connexion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="HomeActivity"
            android:label="Galerie" />
        <activity
            android:name="CreateAccountActivity"
            android:label="Création de compte" />
        <activity
            android:name="FilterActivity"
            android:label="Choix des filtres"/>
        <activity
            android:name="FormatActivity"
            android:label="Choix des formats"/>
        <activity
            android:name="AdressSelectionActivity"
            android:label="Choix des destinataires"/>
        <activity
            android:name="AdressCreationActivity"
            android:label="Creation de contact"></activity><!-- ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>